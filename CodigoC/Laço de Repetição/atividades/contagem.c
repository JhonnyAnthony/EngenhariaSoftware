#include <stdio.h>

int main(int argc, char const *argv[])
{
    int count = 0;
    while (count < 5)
    {
        printf("Contagem: %d\n", count);
        count++;
    }
    
    return 0;

}
